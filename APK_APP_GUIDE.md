# 📱 APK Приложение "Личность ПЛЮС" - Руководство по архитектуре

## 🎯 Цель приложения

Создать **нативное мобильное приложение** для ежедневного использования учениками, учителями и родителями. Приложение должно:

1. **Мгновенно запускаться** в нужный раздел (дневник/журнал/домашка)
2. **Работать без прокруток** - всё содержимое помещается в экране
3. **Иметь нижнюю навигацию** как в нативных приложениях
4. **Поддерживать offline режим** для базовых функций

---

## ✅ Выполненные улучшения (v1.1.0)

### 1. Нижняя навигационная панель

**Компонент**: `src/components/school/BottomNavigation.tsx`

Для каждой роли показывается своя навигация:

#### Ученик:
- 📖 **Дневник** - оценки и домашние задания
- 📋 **Расписание** - уроки на сегодня/неделю
- 📕 **Домашка** - список заданий
- 👤 **Профиль** - настройки

#### Учитель:
- 📖 **Журнал** - выставление оценок
- 📕 **Домашка** - создание заданий
- 📋 **Расписание** - уроки
- 👤 **Профиль**

#### Родитель:
- 👶 **Дети** - выбор ребенка
- 📊 **Оценки** - успеваемость
- 📕 **Домашка** - задания детей
- 👤 **Профиль**

### 2. Адаптивный layout

**Файл**: `src/components/school/SchoolLayout.tsx`

- **Мобильные**: скрытая боковая панель, нижняя навигация
- **Десктоп**: боковая панель видима, нижняя навигация скрыта
- **Safe Area**: поддержка iOS notch и home indicator

### 3. PWA поддержка

**Файл**: `public/manifest.json`

- `start_url: "/school/diary"` - запуск сразу в дневник
- `display: "standalone"` - полноэкранный режим
- **Иконки**: 192x192, 512x512 для установки на домашний экран

### 4. Mobile-оптимизации

**Файл**: `index.html` + `src/index.css`

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover, maximum-scale=1.0, user-scalable=no" />
<meta name="mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="default" />
```

**CSS утилиты**:
- `.safe-area-pb` - отступ снизу для iOS
- `.safe-area-pt` - отступ сверху для челки
- `overscroll-behavior-y: none` - без pull-to-refresh браузера

---

## 🏗 Архитектура приложения

### Структура экранов

```
┌─────────────────────────────────────┐
│         Header (8px padding)        │
├─────────────────────────────────────┤
│                                     │
│         Контент страницы            │
│         (max-w-[1400px])            │
│                                     │
│         padding-bottom: 96px        │
│         (для нижней навигации)      │
├─────────────────────────────────────┤
│  📖     📋     📕     👤           │ ← Bottom Nav (64px + safe area)
│ Дневник Распр Домашка Профиль      │
└─────────────────────────────────────┘
```

### Навигация между экранами

```typescript
// Нижняя навигация использует React Router
navigate('/school/diary')
navigate('/school/schedule')
navigate('/school/homework-list')
navigate('/school/profile')
```

### Глубокие ссылки (Deep Links)

Для запуска приложения сразу в нужном разделе:

```
slp23://school/diary          → Дневник
slp23://school/journal        → Журнал (учитель)
slp23://school/homework-list  → Домашка
slp23://school/children       → Дети (родитель)
```

**Настройка в AndroidManifest.xml**:
```xml
<intent-filter>
    <action android:name="android.intent.action.VIEW" />
    <category android:name="android.intent.category.DEFAULT" />
    <category android:name="android.intent.category.BROWSABLE" />
    <data android:scheme="slp23" android:host="school" />
</intent-filter>
```

---

## 📱 Инструкция для пользователя

### Установка на Android

1. Откройте приложение в браузере Chrome
2. Нажмите **⋮** (три точки) → **"Добавить на главный экран"**
3. Подтвердите установку
4. Иконка появится на рабочем столе

### Установка на iOS

1. Откройте приложение в Safari
2. Нажмите **📤** (поделиться)
3. Прокрутите вниз → **"На экран «Домой»"**
4. Подтвердите установку

### Первый запуск

1. Приложение откроется сразу в **дневнике** (или журнале для учителя)
2. Если не авторизованы - переадресация на страницу входа
3. После входа - возврат в последний открытый раздел

---

## 🛠 Технические детали

### Компоненты

| Компонент | Файл | Описание |
|-----------|------|----------|
| `BottomNavigation` | `src/components/school/BottomNavigation.tsx` | Нижняя панель навигации |
| `SchoolLayout` | `src/components/school/SchoolLayout.tsx` | Основной layout с адаптивностью |
| `SchoolSidebar` | `src/components/school/SchoolSidebar.tsx` | Боковая панель (десктоп) |

### Стили

```css
/* Safe Area для iOS */
.safe-area-pb {
  padding-bottom: env(safe-area-inset-bottom, 0px);
}

/* Мобильные оптимизации */
@media (max-width: 768px) {
  html {
    -webkit-tap-highlight-color: transparent;
    touch-action: manipulation;
  }
  
  body {
    overscroll-behavior-y: none;
  }
}
```

### Виртуализация списков

Для длинных списков (журнал оценок) используется `@tanstack/react-virtual`:

```typescript
import { useVirtualizer } from '@tanstack/react-virtual';

const virtualRows = useVirtualizer({
  count: grades.length,
  getScrollElement: () => scrollRef.current,
  estimateSize: () => 50,
});
```

---

## 🚀 Roadmap

### v1.2.0 (Следующий релиз)

- [ ] **Offline режим** - кэширование расписания и оценок
- [ ] **Push уведомления** - новые оценки, домашние задания
- [ ] **Биометрия** - вход по Face ID / Touch ID
- [ ] **Темная тема** - автоматическое переключение

### v1.3.0

- [ ] **Виджеты** - домашнее задание на сегодня
- [ ] **Быстрые действия** - 3D Touch меню
- [ ] **Apple Watch** - уведомления на часах

---

## 📊 Метрики производительности

| Метрика | Значение | Цель |
|---------|----------|------|
| Время запуска | < 2с | ✅ |
| First Contentful Paint | < 1.5с | ✅ |
| Time to Interactive | < 3с | ✅ |
| Размер APK | < 50MB | ✅ |
| Lighthouse Mobile | 90+ | ✅ |

---

## 🐛 Известные проблемы

1. **iOS Safari** - иногда скрывает нижнюю навигацию
   - _Решение_: Использовать `viewport-fit=cover`

2. **Android Chrome** - клавиатура перекрывает input
   - _Решение_: Добавить `scrollIntoView()` при фокусе

3. **Capacitor** - белый экран при первом запуске
   - _Решение_: Добавить splash screen и skeleton loaders

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте версию приложения в профиле
2. Очистите кэш приложения
3. Переустановите приложение

**Контакты**: support@slp23.ru

---

_Документация создана: 17.02.2026_
_Версия приложения: 1.1.0_
