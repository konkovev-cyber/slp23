# 🚀 Быстрый старт - Сборка APK

## ✅ Что уже сделано:

1. ✅ **Проект собран** (`npm run build`)
2. ✅ **Android синхронизирован** (`npx cap sync android`)
3. ✅ **Иконка и сплэш-скрин** созданы
4. ✅ **Название приложения** исправлено на "Личность ПЛЮС"

---

## 📱 Следующие шаги:

### 1. Открыть проект в Android Studio

```bash
npx cap open android
```

**Или вручную:**
1. Откройте Android Studio
2. File → Open
3. Выберите папку `d:\1_sites\slp23\android`

### 2. Собрать APK

В Android Studio:
1. **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. Дождитесь сообщения "BUILD SUCCESSFUL"
3. Нажмите "locate" в уведомлении

**APK будет здесь:**
```
d:\1_sites\slp23\android\app\build\outputs\apk\debug\app-debug.apk
```

### 3. Установить на телефон

**Способ 1: Через USB (если телефон подключен)**
```bash
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

**Способ 2: Через файл**
1. Скопируйте `app-debug.apk` на телефон
2. Откройте файл на телефоне
3. Разрешите установку из неизвестных источников
4. Нажмите "Установить"

---

## 🧪 Тестирование приложения

### Быстрый тест (без БД):

1. **Запустите приложение** на телефоне
2. **Проверьте иконку** - должна быть с глобусом и шапочкой
3. **Проверьте сплэш-скрин** - должен показаться при запуске
4. **Откройте главную страницу** - сайт должен загрузиться

### Полный тест (с регистрацией):

Следуйте инструкции в файле **`TESTING_GUIDE.md`**

---

## 📊 Статус базы данных

⚠️ **Внимание**: Функция `exec_sql` не найдена в Supabase.

### Альтернативный способ загрузки SQL:

1. Откройте **Supabase Dashboard**: https://supabase.com/dashboard
2. Перейдите в **SQL Editor**
3. Создайте новый запрос
4. Скопируйте содержимое файла `school_diary_schema.sql`
5. Вставьте и выполните (Run)
6. Повторите для `school_test_data.sql`

---

## 🎯 Что тестировать на телефоне:

### Обязательно:
- [ ] Приложение запускается
- [ ] Иконка отображается корректно
- [ ] Сплэш-скрин показывается
- [ ] Главная страница загружается
- [ ] Навигация работает
- [ ] Формы адаптированы под экран

### Дополнительно (после настройки БД):
- [ ] Регистрация работает
- [ ] Вход работает
- [ ] Дневник отображается
- [ ] Профиль редактируется
- [ ] Все кнопки кликабельны

---

## 🔧 Если что-то не работает:

### APK не собирается:
```bash
# Очистите кэш
cd android
./gradlew clean

# Пересоберите
cd ..
npm run build
npx cap sync android
npx cap open android
```

### Приложение вылетает:
1. Проверьте логи в Android Studio (Logcat)
2. Убедитесь, что интернет есть на телефоне
3. Проверьте, что Supabase URL правильный

### База данных не работает:
1. Загрузите SQL через Supabase Dashboard
2. Проверьте RLS-политики
3. Убедитесь, что таблицы созданы

---

## 📦 Файлы проекта:

- `school_diary_schema.sql` - Структура БД
- `school_test_data.sql` - Тестовые данные
- `TESTING_GUIDE.md` - Полная инструкция по тестированию
- `DIARY_ARCHITECTURE.md` - Документация архитектуры
- `android/app/build/outputs/apk/debug/app-debug.apk` - Готовый APK

---

## ✨ Версия приложения: 1.0.1

**Дата сборки**: 17.02.2026  
**Название**: Личность ПЛЮС  
**Package**: ru.slp23.app
