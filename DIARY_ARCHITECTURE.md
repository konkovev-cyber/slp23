# 📚 Архитектура школьного дневника

## Обзор системы

Электронный дневник "Личность ПЛЮС" — это полнофункциональная система управления учебным процессом с поддержкой ролей, расписания, оценок и домашних заданий.

---

## 🎭 Роли пользователей

### 1. **Student (Ученик)**
- Просмотр дневника (расписание + оценки + ДЗ)
- Просмотр расписания
- Просмотр оценок по предметам
- Просмотр домашних заданий
- Редактирование своего профиля

### 2. **Teacher (Учитель)**
- Ведение журнала (выставление оценок)
- Создание домашних заданий
- Просмотр расписания
- Просмотр списка учеников в классах

### 3. **Parent (Родитель)**
- Просмотр дневников своих детей
- Просмотр оценок детей
- Связь с учителями (в будущем)

### 4. **Admin (Администратор)**
- Полный доступ ко всем функциям
- Управление пользователями (одобрение регистраций)
- Управление классами
- Управление расписанием
- Управление предметами
- Назначение учителей на предметы

---

## 🗄️ Структура базы данных

### Таблица `profiles`
```sql
- id: UUID (PK)
- auth_id: UUID (FK -> auth.users)
- role: ENUM('admin', 'teacher', 'student', 'parent')
- is_approved: BOOLEAN (по умолчанию false)
- full_name: TEXT
- avatar_url: TEXT
- phone: TEXT
```

### Таблица `school_classes`
```sql
- id: UUID (PK)
- name: TEXT (например, "10А")
- teacher_id: UUID (FK -> profiles, классный руководитель)
```

### Таблица `subjects`
```sql
- id: UUID (PK)
- name: TEXT (например, "Математика")
```

### Таблица `student_enrollment`
```sql
- id: UUID (PK)
- student_id: UUID (FK -> profiles)
- class_id: UUID (FK -> school_classes)
- UNIQUE(student_id) — один ученик в одном классе
```

### Таблица `timetable` (Расписание)
```sql
- id: UUID (PK)
- class_id: UUID (FK -> school_classes)
- subject_id: UUID (FK -> subjects)
- teacher_id: UUID (FK -> profiles)
- day_of_week: INT (1=Пн, 7=Вс)
- lesson_number: INT (1, 2, 3...)
- start_time: TIME
- room: TEXT (опционально)
```

### Таблица `journal_grades` (Оценки)
```sql
- id: UUID (PK)
- student_id: UUID (FK -> profiles)
- subject_id: UUID (FK -> subjects)
- teacher_id: UUID (FK -> profiles, кто поставил)
- grade: TEXT ("5", "4", "3", "2", "Н")
- comment: TEXT (опционально)
- graded_at: DATE
```

### Таблица `homework` (Домашние задания)
```sql
- id: UUID (PK)
- class_id: UUID (FK -> school_classes)
- subject_id: UUID (FK -> subjects)
- description: TEXT
- due_date: DATE
- attachments: TEXT[] (ссылки на файлы)
```

---

## 🔐 Процесс регистрации и одобрения

1. **Регистрация** (`/school/signup`):
   - Пользователь вводит email, пароль, ФИО
   - Выбирает роль (ученик/родитель/учитель)
   - Создается аккаунт с `is_approved = false`

2. **Ожидание** (`/school/pending`):
   - Пользователь видит экран ожидания
   - Может обновить статус вручную

3. **Одобрение администратором**:
   - Админ заходит в `/school/admin/users`
   - Видит список неподтвержденных пользователей
   - Одобряет или отклоняет регистрацию
   - При одобрении `is_approved = true`

4. **Доступ**:
   - После одобрения пользователь получает доступ к дневнику

---

## 📱 Страницы приложения

### Для учеников:
- `/school/diary` — Дневник (день/неделя/месяц)
- `/school/grades` — Все оценки по предметам
- `/school/schedule` — Расписание
- `/school/homework-list` — Список ДЗ
- `/school/profile` — Профиль

### Для учителей:
- `/school/journal` — Журнал (выставление оценок)
- `/school/homework` — Создание ДЗ
- `/school/schedule` — Расписание

### Для родителей:
- `/school/children` — Список детей
- Переход к дневнику ребенка

### Для администраторов:
- `/school/admin/users` — Управление пользователями
- `/school/admin/classes` — Управление классами
- `/school/admin/schedule` — Управление расписанием
- `/school/admin/grades` — Просмотр всех оценок

---

## 🎨 UI/UX особенности

### Адаптивность:
- **Мобильные устройства**: Все формы адаптированы под экраны 320px+
- **Планшеты**: Оптимизированная сетка для средних экранов
- **Десктоп**: Полноценный интерфейс с боковой панелью

### Дизайн:
- **Цветовая схема**: Slate + Primary (оранжевый)
- **Скругления**: 16-40px для премиального вида
- **Тени**: Многослойные тени для глубины
- **Анимации**: Плавные переходы и hover-эффекты

### Формы:
- Все инпуты имеют высоту 56-64px для удобства на мобильных
- Кнопки крупные (минимум 48px высотой)
- Четкие лейблы и подсказки

---

## 🔄 Алгоритм работы дневника

### Загрузка данных (день):
1. Получить `class_id` ученика из `student_enrollment`
2. Получить расписание на выбранный день из `timetable`
3. Для каждого урока:
   - Загрузить название предмета
   - Загрузить ФИО учителя
   - Найти оценку за этот день по этому предмету
   - Найти ДЗ с `due_date` = выбранная дата

### Загрузка данных (неделя):
- То же самое, но для 7 дней (Пн-Вс)
- Группировка по дням

### Загрузка данных (месяц):
- Календарная сетка
- Отметки дней с оценками
- Клик по дню → переход в режим "день"

---

## 🚀 Следующие шаги

### Критично:
- [ ] Исправить адаптацию форм на мобильных (max-width, overflow)
- [ ] Добавить кнопки "Сохранить" и "Отмена" в диалоги
- [ ] Реализовать страницу одобрения пользователей для админа
- [ ] Протестировать регистрацию → одобрение → вход

### Важно:
- [ ] Добавить уведомления об обновлениях приложения
- [ ] Реализовать связь родитель-ребенок
- [ ] Добавить фильтры и поиск в журнале
- [ ] Экспорт оценок в Excel/PDF

### Желательно:
- [ ] Push-уведомления о новых оценках
- [ ] Чат с учителями
- [ ] Электронный дневник посещаемости
- [ ] Интеграция с расписанием звонков

---

## 📦 Сборка APK

```bash
# 1. Билд веб-версии
npm run build

# 2. Синхронизация с Capacitor
npx cap sync android

# 3. Открыть в Android Studio
npx cap open android

# 4. Build -> Build Bundle(s) / APK(s) -> Build APK(s)
```

---

## 🐛 Известные проблемы

1. **Опечатка в названии**: "ичность С" → исправлено на "Личность ПЛЮС"
2. **Формы не помещаются**: Нужно добавить `overflow-y-auto` и `max-h-[80vh]`
3. **Нет кнопок сохранения**: Добавлены в диалоги профиля
4. **Регистрация без проверки**: Добавлен флаг `is_approved`

---

**Версия документации**: 1.0.1  
**Дата**: 17.02.2026  
**Автор**: Antigravity AI
