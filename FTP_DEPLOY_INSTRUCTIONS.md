# 🚀 FTP Деплой - Правильная инструкция

## ✅ Рабочий процесс деплоя на slp23.ru

### Предварительная настройка (один раз)

**1. Файл `.env.local`** (в корне проекта):

```env
FTP_HOST="konkevlk.beget.tech"
FTP_USER="konkevlk_boss"
FTP_PASSWORD="Kk1478963!!!"
FTP_REMOTE_PATH="/slp23.ru/public_html/"
```

**⚠️ Важно:** 
- `.env.local` должен быть в `.gitignore` (не коммитьте!)
- Путь `/slp23.ru/public_html/` — правильный для этого хостинга

**2. Установите зависимости:**

```bash
npm install basic-ftp dotenv archiver
```

---

## 📋 Процесс деплоя (каждый раз)

### Шаг 1: Сборка проекта

```bash
npm run build
```

**Результат:** Папка `dist/` с готовыми файлами

---

### Шаг 2: Загрузка архива на FTP

```bash
node ftp-deploy.js
```

**Что делает:**
- Создаёт `deploy.zip` из папки `dist/`
- Загружает в `/slp23.ru/public_html/deploy.zip`
- Удаляет локальный `deploy.zip`

**Вывод:**
```
✅ ДЕПЛОЙ ЗАВЕРШЁН!
📝 Откройте https://slp23.ru/_unzip.php для распаковки
```

---

### Шаг 3: Загрузка _unzip.php на FTP

```bash
node upload-unzip.js
```

**Что делает:**
- Загружает `_unzip.php` в `/slp23.ru/public_html/_unzip.php`

**⚠️ Важно:** Оба файла (`deploy.zip` и `_unzip.php`) должны быть в **одной папке** на сервере!

---

### Шаг 4: Распаковка архива

**Откройте в браузере:**
```
https://slp23.ru/_unzip.php
```

**Или через curl:**
```bash
curl https://slp23.ru/_unzip.php
```

**Ожидаемый ответ:**
```
📦 Распаковка deploy.zip...
✅ Распаковка успешна!
🗑️  Архив удалён

✅ ДЕПЛОЙ ЗАВЕРШЁН!
📝 Удалите этот файл (_unzip.php) для безопасности
```

---

### Шаг 5: Удаление _unzip.php с сервера

```bash
node remove-unzip.js
```

**Что делает:**
- Подключается к FTP
- Удаляет `/slp23.ru/public_html/_unzip.php`

**Вывод:**
```
✅ _unzip.php удалён!

🎉 ДЕПЛОЙ ПОЛНОСТЬЮ ЗАВЕРШЁН!
🌐 Сайт доступен: https://slp23.ru
```

---

## 🎯 Быстрая команда (всё в одном)

```bash
npm run build && node ftp-deploy.js && node upload-unzip.js
```

Затем откройте в браузере:
```
https://slp23.ru/_unzip.php
```

После распаковки:
```bash
node remove-unzip.js
```

---

## 🔧 Настройка package.json

Добавьте скрипты для удобного деплоя:

```json
{
  "scripts": {
    "build": "vite build",
    "deploy": "npm run build && node ftp-deploy.js && node upload-unzip.js",
    "deploy:finalize": "node remove-unzip.js"
  }
}
```

**Использование:**
```bash
# Полный деплой
npm run deploy

# Распаковка (в браузере)
# Откройте https://slp23.ru/_unzip.php

# Удаление _unzip.php
npm run deploy:finalize
```

---

## 📁 Структура файлов

```
slp23/
├── .env.local              # FTP учётные данные (НЕ КОММИТИТЬ!)
├── .gitignore              # Должен содержать .env.local
├── ftp-deploy.js           # Загрузка deploy.zip
├── upload-unzip.js         # Загрузка _unzip.php
├── remove-unzip.js         # Удаление _unzip.php
├── _tools/deploy-config/
│   └── _unzip.php          # Скрипт распаковки (исходник)
└── dist/                   # Папка сборки (удаляется перед новой сборкой)
```

---

## 🔐 Безопасность

1. **`.env.local` в `.gitignore`** — уже добавлен
2. **Удаление `_unzip.php`** — обязательно после распаковки
3. **FTPS вместо FTP** — измените `secure: false` на `true` в `ftp-deploy.js` если хостинг поддерживает
4. **Сложные пароли** — используйте надёжные пароли для FTP

---

## 🛠️ Решение проблем

### Ошибка: "553 Could not create file"

**Проблема:** Неправильный путь FTP

**Решение:** Проверьте `FTP_REMOTE_PATH` в `.env.local`:
```env
FTP_REMOTE_PATH="/slp23.ru/public_html/"
```

---

### Ошибка: "_unzip.php not found"

**Проблема:** Файл `_unzip.php` и `deploy.zip` в разных папках

**Решение:** Убедитесь, что оба файла загружены в одну папку:
```bash
node ftp-deploy.js    # Загружает deploy.zip
node upload-unzip.js  # Загружает _unzip.php (в ту же папку!)
```

---

### Ошибка: "FTP connected but upload fails"

**Проблема:** Нет прав на запись

**Решение:**
1. Проверьте логин/пароль в `.env.local`
2. Убедитесь, что путь начинается с `/`
3. Проверьте права на папку через файловый менеджер хостинга

---

### Ошибка: "_unzip.php не работает"

**Проблема:** PHP не поддерживает ZipArchive

**Решение:**
1. Проверьте поддержку ZipArchive в PHP
2. Откройте `phpinfo()` на сервере
3. Убедитесь, что расширение `zip` включено

---

## 📝 Чек-лист деплоя

- [ ] Изменения закоммичены в Git
- [ ] `npm run build` выполнен успешно
- [ ] `node ftp-deploy.js` загрузил архив
- [ ] `node upload-unzip.js` загрузил скрипт распаковки
- [ ] Открыт `https://slp23.ru/_unzip.php` в браузере
- [ ] Распаковка успешна (видите сообщение "✅ ДЕПЛОЙ ЗАВЕРШЁН!")
- [ ] `node remove-unzip.js` удалил скрипт распаковки
- [ ] Сайт проверен в браузере (Ctrl+F5 для очистки кэша)

---

## 🌐 Полезные ссылки

- **FTP хост:** `konkevlk.beget.tech`
- **FTP логин:** `konkevlk_boss`
- **FTP путь:** `/slp23.ru/public_html/`
- **Сайт:** https://slp23.ru
- **_unzip.php:** https://slp23.ru/_unzip.php

---

## 📞 Поддержка

Если что-то не работает:

1. Проверьте `.env.local` — правильность учётных данных
2. Запустите с подробными логами: `node ftp-deploy.js`
3. Проверьте логи хостинга (через панель управления Beget)
4. Убедитесь, что `deploy.zip` и `_unzip.php` в одной папке

---

**Создано:** 19 февраля 2026  
**Проект:** slp23.ru  
**Хостинг:** BeGet (konkevlk.beget.tech)
