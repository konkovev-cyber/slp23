# 🧪 Инструкция по тестированию школьного дневника

## Подготовка

### 1. Запуск SQL-скриптов

```bash
# В корне проекта выполните:
node -e "const { createClient } = require('@supabase/supabase-js'); const s = createClient('https://qwuicyhadpesklhkjxpn.supabase.co', 'sb_secret_HzH8nQFmVqlvM95VQGoJnQ_y2FfA5vC'); const fs = require('fs'); const sql = fs.readFileSync('school_diary_schema.sql', 'utf8'); console.log('Executing schema...'); s.rpc('exec_sql', { sql }).then(r => console.log('Schema:', r.error ? r.error.message : 'Success'))"

# Затем тестовые данные:
node -e "const { createClient } = require('@supabase/supabase-js'); const s = createClient('https://qwuicyhadpesklhkjxpn.supabase.co', 'sb_secret_HzH8nQFmVqlvM95VQGoJnQ_y2FfA5vC'); const fs = require('fs'); const sql = fs.readFileSync('school_test_data.sql', 'utf8'); console.log('Loading test data...'); s.rpc('exec_sql', { sql }).then(r => console.log('Data:', r.error ? r.error.message : 'Success'))"
```

---

## 📝 Сценарий тестирования

### Шаг 1: Регистрация администратора

1. Откройте `/admin`
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - **ФИО**: Иванов Иван Иванович
   - **Email**: admin@school.test
   - **Пароль**: admin123
   - **Роль**: Ученик (временно, потом изменим на админа)
4. Нажмите "Зарегистрироваться"

### Шаг 2: Назначение роли админа вручную

Так как это первый пользователь, нужно вручную назначить роль админа:

```sql
-- Выполните в Supabase SQL Editor:
UPDATE public.profiles 
SET role = 'admin', is_approved = true 
WHERE auth_id = (SELECT id FROM auth.users WHERE email = 'admin@school.test');
```

### Шаг 3: Вход как администратор

1. Перейдите на `/admin`
2. Войдите с email: `admin@school.test`, пароль: `admin123`
3. Вы должны попасть в админ-панель

### Шаг 4: Регистрация учителя

1. Откройте `/school/signup` в режиме инкогнито (или другом браузере)
2. Заполните форму:
   - **ФИО**: Петрова Мария Сергеевна
   - **Email**: teacher@school.test
   - **Пароль**: teacher123
   - **Роль**: Учитель
3. Нажмите "Зарегистрироваться"
4. Вы должны попасть на страницу `/school/pending` с сообщением об ожидании

### Шаг 5: Одобрение учителя администратором

1. В основном браузере (где вы админ) перейдите на `/school/admin/users`
2. Вы должны увидеть Петрову М.С. в списке "Ожидают одобрения"
3. Нажмите кнопку "Одобрить"
4. Статус должен измениться на "Одобрен"

### Шаг 6: Вход учителя

1. В режиме инкогнито обновите страницу `/school/pending`
2. Нажмите "Проверить статус"
3. Вас должно перенаправить в дневник `/school/diary`

### Шаг 7: Регистрация ученика

1. Откройте `/school/signup` в третьем браузере/профиле
2. Заполните форму:
   - **ФИО**: Сидоров Петр Александрович
   - **Email**: student@school.test
   - **Пароль**: student123
   - **Роль**: Ученик
3. Зарегистрируйтесь
4. Одобрите через админ-панель

### Шаг 8: Привязка ученика к классу

```sql
-- Получите auth_id ученика
SELECT auth_id, full_name FROM public.profiles WHERE email = 'student@school.test';

-- Получите ID класса 10А
SELECT id FROM public.school_classes WHERE name = '10А';

-- Привяжите ученика к классу (замените UUID на реальные)
INSERT INTO public.students_info (student_id, class_id)
VALUES ('UUID_УЧЕНИКА', 'UUID_КЛАССА_10А');
```

### Шаг 9: Создание расписания

```sql
-- Получите UUID учителя и предметов
SELECT auth_id FROM public.profiles WHERE email = 'teacher@school.test';
SELECT id, name FROM public.subjects WHERE name IN ('Математика', 'Русский язык', 'Физика');

-- Используйте функцию для добавления уроков (замените UUID):
SELECT add_test_schedule('10А', 'UUID_УЧИТЕЛЯ', 'Математика', 1, 1, '08:00');
SELECT add_test_schedule('10А', 'UUID_УЧИТЕЛЯ', 'Русский язык', 1, 2, '09:00');
SELECT add_test_schedule('10А', 'UUID_УЧИТЕЛЯ', 'Физика', 1, 3, '10:00');

-- Добавьте уроки на вторник (day_of_week = 2)
SELECT add_test_schedule('10А', 'UUID_УЧИТЕЛЯ', 'Математика', 2, 1, '08:00');
SELECT add_test_schedule('10А', 'UUID_УЧИТЕЛЯ', 'Физика', 2, 2, '09:00');
```

### Шаг 10: Добавление оценок

```sql
-- Получите UUID ученика и учителя
SELECT auth_id FROM public.profiles WHERE email = 'student@school.test';
SELECT auth_id FROM public.profiles WHERE email = 'teacher@school.test';

-- Добавьте оценки (замените UUID):
SELECT add_test_grade('UUID_УЧЕНИКА', 'UUID_УЧИТЕЛЯ', 'Математика', '5', 'Отлично!', CURRENT_DATE);
SELECT add_test_grade('UUID_УЧЕНИКА', 'UUID_УЧИТЕЛЯ', 'Русский язык', '4', 'Хорошо', CURRENT_DATE);
SELECT add_test_grade('UUID_УЧЕНИКА', 'UUID_УЧИТЕЛЯ', 'Физика', '5', NULL, CURRENT_DATE - INTERVAL '1 day');
```

### Шаг 11: Добавление домашних заданий

```sql
-- Добавьте ДЗ (замените UUID учителя):
SELECT add_test_homework(
    'UUID_УЧИТЕЛЯ',
    'Математика',
    'Решить задачи',
    'Учебник стр. 45, №123-130. Принести тетрадь для контрольных работ.',
    CURRENT_DATE + INTERVAL '2 days'
);

SELECT add_test_homework(
    'UUID_УЧИТЕЛЯ',
    'Русский язык',
    'Сочинение',
    'Написать сочинение на тему "Мой любимый писатель" (200-300 слов)',
    CURRENT_DATE + INTERVAL '5 days'
);
```

### Шаг 12: Проверка дневника ученика

1. Войдите как ученик (`student@school.test`)
2. Перейдите на `/school/diary`
3. **Проверьте режим "День"**:
   - Должны отображаться уроки на сегодня
   - Должны быть видны оценки
   - Должны быть видны домашние задания
4. **Проверьте режим "Неделя"**:
   - Должна отображаться вся неделя
   - Уроки сгруппированы по дням
5. **Проверьте режим "Месяц"**:
   - Календарная сетка
   - Дни с оценками отмечены точкой

### Шаг 13: Проверка мобильной версии

1. Откройте Chrome DevTools (F12)
2. Включите режим устройства (Ctrl+Shift+M)
3. Выберите iPhone 12 Pro
4. Проверьте:
   - ✅ Форма регистрации помещается на экран
   - ✅ Диалог редактирования профиля прокручивается
   - ✅ Кнопки "Сохранить" и "Отмена" видны
   - ✅ Дневник адаптирован под мобильный экран

---

## 📦 Сборка APK

### Шаг 1: Билд проекта

```bash
npm run build
```

### Шаг 2: Синхронизация с Android

```bash
npx cap sync android
```

### Шаг 3: Открыть в Android Studio

```bash
npx cap open android
```

### Шаг 4: Сборка APK

1. В Android Studio: **Build → Build Bundle(s) / APK(s) → Build APK(s)**
2. Дождитесь завершения сборки
3. APK будет в `android/app/build/outputs/apk/debug/app-debug.apk`

### Шаг 5: Установка на устройство

**Вариант А: Через USB**
```bash
adb install android/app/build/outputs/apk/debug/app-debug.apk
```

**Вариант Б: Через файл**
1. Скопируйте APK на телефон
2. Откройте файл на телефоне
3. Разрешите установку из неизвестных источников
4. Установите

---

## ✅ Чек-лист тестирования

### Регистрация и авторизация
- [ ] Регистрация нового пользователя работает
- [ ] Выбор роли сохраняется
- [ ] Страница ожидания отображается корректно
- [ ] Одобрение администратором работает
- [ ] После одобрения пользователь может войти
- [ ] Вход с неверными данными показывает ошибку

### Дневник ученика
- [ ] Отображается расписание на день
- [ ] Отображается расписание на неделю
- [ ] Отображается календарь на месяц
- [ ] Оценки отображаются корректно
- [ ] Домашние задания отображаются
- [ ] Переключение между режимами работает
- [ ] Навигация по датам работает

### Профиль
- [ ] Редактирование имени работает
- [ ] Смена аватара (пресеты) работает
- [ ] Загрузка своего аватара работает
- [ ] Изменения сохраняются
- [ ] Кнопка "Отмена" работает

### Админ-панель
- [ ] Список пользователей загружается
- [ ] Фильтры работают (все/ожидают/одобренные)
- [ ] Одобрение пользователя работает
- [ ] Отклонение пользователя работает
- [ ] Смена роли работает
- [ ] Статистика отображается корректно

### Мобильная версия
- [ ] Все формы помещаются на экран
- [ ] Диалоги прокручиваются
- [ ] Кнопки доступны
- [ ] Навигация работает
- [ ] Текст читабелен

### APK
- [ ] APK собирается без ошибок
- [ ] Приложение устанавливается на телефон
- [ ] Иконка отображается корректно
- [ ] Сплэш-скрин показывается
- [ ] Приложение запускается
- [ ] Все функции работают на реальном устройстве

---

## 🐛 Известные проблемы и решения

### Проблема: "Column id does not exist"
**Решение**: Используйте обновленный `school_diary_schema.sql` с `auth_id` вместо `id`

### Проблема: Пользователь не может войти после одобрения
**Решение**: Проверьте, что `is_approved = true` в таблице `profiles`

### Проблема: Расписание не отображается
**Решение**: Убедитесь, что ученик привязан к классу через `students_info`

### Проблема: Оценки не показываются
**Решение**: Проверьте, что создана запись в `teacher_assignments` для связи учителя и предмета

### Проблема: APK не устанавливается
**Решение**: 
1. Проверьте, что включена установка из неизвестных источников
2. Удалите старую версию приложения
3. Пересоберите APK

### Проблема: Ошибка сборки "terser not found"
**Решение**: Установите недостающую зависимость:
```bash
npm install -D terser
```

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12)
2. Проверьте логи Supabase
3. Убедитесь, что все SQL-скрипты выполнены успешно
4. Проверьте RLS-политики в Supabase

**Версия**: 1.0.1  
**Дата**: 17.02.2026
