# 📋 Настройка Keenetic как VLESS клиента

## ⚠️ ВАЖНО: Сначала восстановите работу!

Если после моих изменений интернет пропал:

### Способ 1: Сброс настроек роутера
1. Зажмите кнопку **Reset** на роутере на 10 секунд
2. Роутер перезагрузится с заводскими настройками
3. Настройте интернет заново

### Способ 2: Отключить VPN в веб-интерфейсе
1. Подключитесь к роутеру по кабелю
2. Откройте `http://192.168.100.1` или `http://my.keenetic.net`
3. Перейдите в **Приложения → VLESS/Xray**
4. **Отключите** переключатель VPN
5. Сохраните

---

## 🔧 Правильная настройка VLESS на Keenetic

### Шаг 1: Получите данные из 3X-UI на сервере

Подключитесь к серверу по SSH и выполните:

```bash
# Показать список пользователей
cd /usr/local/x-ui
./x-ui

# Выберите пункт "Показать клиентов" или "Manage Clients"
# Скопируйте: UUID, port, reality settings
```

Или через базу данных:
```bash
sqlite3 /usr/local/x-ui/x-ui.db "SELECT id, settings FROM inbounds;"
```

### Шаг 2: Заполните конфиг

Откройте файл `vless-client-config.json` и замените:

| Параметр | Где взять |
|----------|-----------|
| `ВАШ_UUID_ИЗ_3X-UI` | В 3X-UI интерфейсе → Inbounds → Edit → Users → UUID |
| `ВАШ_PUBLIC_KEY_ИЗ_3X-UI` | В 3X-UI → Reality Settings → Public Key |
| `ВАШ_SHORT_ID_ИЗ_3X-UI` | В 3X-UI → Reality Settings → Short ID |

### Шаг 3: Настройка в веб-интерфейсе Keenetic

1. Откройте `http://192.168.100.1`
2. **Приложения → VLESS/Xray**
3. Загрузите файл `vless-client-config.json`
4. Включите VPN
5. Сохраните

---

## 🧪 Проверка работы

### 1. Проверка подключения к серверу:
```bash
# В командной строке Keenetic (если есть SSH доступ)
ping 23fito.ru
curl -I https://23fito.ru
```

### 2. Проверка маршрутизации:
```bash
# Должен идти напрямую:
curl -I https://gos.roseltorg.ru

# Должен идти через VPN:
curl --socks5 192.168.100.1:7890 https://google.com
```

### 3. Проверка в браузере:
- Откройте `https://23fito.ru` → должна быть GitHub-заглушка
- Откройте `https://gos.roseltorg.ru` → должен открыться портал
- Откройте `https://gemini.google.com` → должен открыться через VPN

---

## 🔍 Диагностика проблем

### Проблема: Интернет пропал после включения VPN

**Решение:**
1. Отключите VPN в веб-интерфейсе
2. Проверьте, появился ли интернет
3. Если да — проблема в конфиге маршрутизации
4. Удалите правило по умолчанию (`"outboundTag": "vless-reality"`) из конца списка

### Проблема: Отдельные сайты не работают

**Решение:**
1. Проверьте логи:
   ```bash
   cat /tmp/xray/error.log
   ```
2. Добавьте домен в нужное правило (direct или vless-reality)

### Проблема: WhatsApp/Telegram не работают

**Решение:**
1. Увеличьте `connIdle` до 600:
   ```json
   "connIdle": 600
   ```
2. Проверьте, что домены в правиле VPN:
   ```json
   "domain": [
     "whatsapp.com",
     "telegram.org",
     "t.me"
   ]
   ```

---

## 📊 Структура маршрутизации

```
┌─────────────────────────────────────────────────┐
│              Все устройства в сети              │
│           (192.168.100.1-254)                   │
└───────────────────┬─────────────────────────────┘
                    │
        ┌───────────▼───────────┐
        │   Keenetic Роутер     │
        │   (VLESS Клиент)      │
        └───────────┬───────────┘
                    │
    ┌───────────────┼───────────────┐
    │               │               │
┌───▼───┐    ┌─────▼─────┐   ┌────▼────┐
│Direct │    │   VPN     │   │  Block  │
│(напр.)│    │(через VPS)│   │(запрет)│
└───────┘    └───────────┘   └─────────┘
    │               │               │
    │               │               │
.ru домены     gemini.google.  UDP 135-139
госпорталы     whatsapp.com    реклама
локальная      telegram.org
сеть 192.168.x
```

---

## 🎯 Минимальный рабочий конфиг (если ничего не помогает)

Если сложная маршрутизация не работает — используйте простой вариант:

```json
{
  "log": {
    "loglevel": "warning"
  },
  "inbounds": [
    {
      "port": 7890,
      "protocol": "socks",
      "settings": {"auth": "noauth", "udp": true}
    }
  ],
  "outbounds": [
    {
      "tag": "vless-reality",
      "protocol": "vless",
      "settings": {
        "vnext": [
          {
            "address": "23fito.ru",
            "port": 443,
            "users": [
              {
                "id": "ВАШ_UUID",
                "flow": "xtls-rprx-vision",
                "encryption": "none"
              }
            ]
          }
        ]
      },
      "streamSettings": {
        "network": "tcp",
        "security": "reality",
        "realitySettings": {
          "serverName": "23fito.ru",
          "fingerprint": "chrome",
          "publicKey": "ВАШ_PUBLIC_KEY",
          "shortId": "ВАШ_SHORT_ID"
        }
      }
    }
  ],
  "routing": {
    "rules": [
      {
        "type": "field",
        "ip": ["geoip:private"],
        "outboundTag": "direct"
      },
      {
        "type": "field",
        "outboundTag": "vless-reality"
      }
    ]
  }
}
```

Этот конфиг отправляет **весь трафик через VPN**, кроме локальной сети.

---

## 📞 Если всё ещё не работает

Пришлите:
1. Скриншот настроек VLESS в Keenetic
2. Вывод команды: `cat /tmp/xray/error.log`
3. Описание: что именно не работает (все сайты или отдельные)
