# План улучшения модуля Admin Media

## Обзор
Модуль админ-медиа (http://localhost:8080/admin/media) требует улучшения интерфейса и функциональности для более удобного управления файлами.

## Текущие проблемы

### AdminMedia.tsx
1. ❌ Нет информации о размере файлов
2. ❌ Нет информации о дате загрузки
3. ❌ Предпросмотр фиксированного размера (aspect-video)
4. ❌ Нет drag-and-drop для загрузки
5. ❌ Нет прогресс-бара загрузки
6. ❌ Нет возможности копировать URL
7. ❌ Нет возможности скачать файл
8. ❌ Нет сортировки файлов
9. ❌ Нет поиска по файлам
10. ❌ Нет массового удаления
11. ❌ Нет информации о типе файла

### ImageUploader.tsx
1. ❌ Стандартный input file выглядит плохо
2. ❌ Нет drag-and-drop зоны
3. ❌ Нет прогресс-бара загрузки
4. ❌ Нет валидации размера файла
5. ❌ Нет информации о размере файла до загрузки

## Решения

### 1. Улучшенный ImageUploader.tsx

**Новые функции:**
- Drag-and-drop зона для загрузки
- Визуальный прогресс-бар загрузки
- Отображение размера файла до загрузки
- Валидация размера файла (максимум 10MB)
- Красивый UI с иконками и анимациями
- Предпросмотр загружаемого файла

**Структура:**
```
┌─────────────────────────────────────────┐
│  📁 Загрузить файл                      │
├─────────────────────────────────────────┤
│  ┌───────────────────────────────────┐  │
│  │                                   │  │
│  │    Перетащите файл сюда           │  │
│  │    или нажмите для выбора         │  │
│  │                                   │  │
│  │    PNG, JPG, WEBP до 10MB         │  │
│  └───────────────────────────────────┘  │
│                                         │
│  [████████░░░░] 75%                     │
│  Загрузка...                            │
└─────────────────────────────────────────┘
```

### 2. Компонент ImageCard.tsx

**Новые функции:**
- Отображение размера файла (KB/MB)
- Отображение даты загрузки
- Отображение типа файла (расширение)
- Кнопка копирования URL
- Кнопка скачивания файла
- Кнопка удаления
- Чекбокс для массового выбора
- Hover эффекты

**Структура:**
```
┌─────────────────────────────────────┐
│  [x]  ┌─────────────────────────┐  │
│       │                         │  │
│       │      Превью             │  │
│       │                         │  │
│       └─────────────────────────┘  │
│                                     │
│  image-1234567890.jpg               │
│  📊 2.4 MB  •  📅 10.02.2026       │
│  🏷️ JPG                             │
│                                     │
│  [📋] [⬇️] [🔗] [🗑️]                │
└─────────────────────────────────────┘
```

### 3. Компонент ImagePreviewDialog.tsx

**Новые функции:**
- Полноэкранный предпросмотр
- Навигация между изображениями (стрелки)
- Информация о файле
- Кнопки действий (копировать, скачать, удалить)
- Закрытие по ESC

### 4. Обновленный AdminMedia.tsx

**Новые функции:**
- Панель инструментов с поиском и сортировкой
- Массовое удаление выбранных файлов
- Улучшенные хлебные крошки
- Статистика хранилища
- Фильтрация по типу файла

**Структура:**
```
┌─────────────────────────────────────────────────────────┐
│  🗄️ Хранилище медиа                    [🔄 Обновить]   │
│  Централизованное управление всеми файлами проекта      │
├─────────────────────────────────────────────────────────┤
│  [Общие] [Новости] [Аватары]                            │
├─────────────────────────────────────────────────────────┤
│  ┌──────────────┐  ┌─────────────────────────────────┐ │
│  │ Навигация    │  │ 🔍 Поиск...  [Сортировка ▼]     │ │
│  │ root > hero  │  ├─────────────────────────────────┤ │
│  │              │  │ [x] Выбрано: 3  [🗑️ Удалить]    │ │
│  │ 📤 Загрузить │  ├─────────────────────────────────┤ │
│  │              │  │ 📁 Папок: 5  📄 Файлов: 23      │ │
│  │ [Drag&Drop]  │  ├─────────────────────────────────┤ │
│  │              │  │ ┌─────┐ ┌─────┐ ┌─────┐         │ │
│  │ Быстрый     │  │ │ img │ │ img │ │ img │ ...     │ │
│  │ переход:    │  │ └─────┘ └─────┘ └─────┘         │ │
│  │ [Главная]   │  │                                 │ │
│  │ [О школе]   │  │                                 │ │
│  │ [Программы] │  │                                 │ │
│  └──────────────┘  └─────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## Технические детали

### Форматирование размера файла
```typescript
function formatFileSize(bytes: number): string {
  if (bytes === 0) return '0 B';
  const k = 1024;
  const sizes = ['B', 'KB', 'MB', 'GB'];
  const i = Math.floor(Math.log(bytes) / Math.log(k));
  return `${(bytes / Math.pow(k, i)).toFixed(1)} ${sizes[i]}`;
}
```

### Форматирование даты
```typescript
function formatDate(dateString: string): string {
  return new Date(dateString).toLocaleDateString('ru-RU', {
    day: '2-digit',
    month: '2-digit',
    year: 'numeric'
  });
}
```

### Получение размера файла из metadata
```typescript
// Supabase хранит размер в metadata.size
const fileSize = file.metadata?.size || 0;
```

## Порядок реализации

1. ✅ Проанализировать текущий код
2. ⏳ Создать улучшенный ImageUploader.tsx
3. ⏳ Создать ImageCard.tsx
4. ⏳ Создать ImagePreviewDialog.tsx
5. ⏳ Обновить AdminMedia.tsx
6. ⏳ Тестирование

## Файлы для создания/обновления

- `src/components/admin/ImageUploader.tsx` - обновить
- `src/components/admin/ImageCard.tsx` - создать новый
- `src/components/admin/ImagePreviewDialog.tsx` - создать новый
- `src/pages/AdminMedia.tsx` - обновить
- `src/lib/utils.ts` - добавить утилиты форматирования
